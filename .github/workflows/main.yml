name: CI/CD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List directory contents
        run: |
          ls
          cd D:\a\video-editing\video-editing\client
          npm install
          npx jest Logout.test.js

